name: 🔄 每周更新龙头股

on:
  schedule:
    - cron: '0 1 * * 1'  # 周一凌晨 1点 (UTC时间，对应北京时间上午9点)
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install akshare pandas -i https://pypi.tuna.tsinghua.edu.cn/simple

      - name: Run update script
        run: |
          python update_leaders.py

      # --- 关键修改点：使用 git diff 判断是否有变更 ---
      - name: Commit and Push
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"

          # 1. 检查文件是否存在且有变化
          if [ -f auto_leaders.json ]; then
            git add auto_leaders.json
            # 如果有修改，才会进行 commit
            if git diff --staged --quiet; then
              echo "ℹ️ auto_leaders.json 无变化，跳过提交"
            else
              git commit -m "Auto update leaders $(date +%Y-%m-%d)"
              git push
            fi
          else
            echo "⚠️ auto_leaders.json 文件未生成！请检查日志。"
            # 如果文件不存在，创建一个空文件防止下次报错（可选）
            # echo "{}" > auto_leaders.json
          fi
