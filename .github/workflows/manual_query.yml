name: 🔍 手动查询主力清单

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - run: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
      - run: python monitor_v3.py
        env:
          MANUAL_QUERY: "true"
